# Read only by `vite --mode fake`, which is what `npm run dev:fake` runs. A plain `vite build` runs
# in production mode and never loads this file, so the playback fake and the dev panel stay out of a
# deployed bundle. `scripts/assert-no-fake-bytes.mjs` is what proves that on every build.
VITE_FAKE_BLE=true
